# ============================================================
#  模板参数 —— 全部可改，模板里通过 site.Params.* 读取
# ============================================================

# 站点副标题，显示在页头 logo 下方
subtitle = ""

# 浏览器标签页 / 卡片图标
favicon = "/favicon.png"

# 首页每页文章数
pagerSize = 8

# 文章正文页显示 "本文共 N 字"
showWordCount = true
# 文章正文页显示预计阅读时间
showReadingTime = true
# 文章正文页显示目录
showTOC = true
# 目录默认是否展开
tocOpen = false

# 文章卡片是否显示摘要
showSummary = true
# 文章卡片是否显示封面图
showCover = true

# 页脚
[footer]
  since = 2020
  customText = ""

# 日期显示格式（Hugo 使用 Go 的时间布局）
[dateFormat]
  published = "2006年01月02日"
  list = "2006-01-02"

# ---- 导航菜单 ----
# 只留三项。文章列表与标签已经在首页卡片和右侧栏都能进，
# 顶栏再放一遍属于重复入口（原本还挤掉了搜索框的宽度）。
[[main]]
  name = "主页"
  url = "/"
  weight = 10

[[main]]
  name = "归档"
  url = "/archives/"
  section = "archives"
  weight = 20

[[main]]
  name = "关于"
  url = "/about/"
  weight = 30

# ---- 顶栏右侧下拉 ----
# 已移除：原来照抄了原站 Bootstrap 的「其他」下拉，交互只是默认态。
# 需要外链入口的话放侧栏 links，或直接加进上面的 [[main]]。

# ---- 侧栏：友情链接 ----
# 侧栏的「友情链接」卡片已按要求移除，数据先留在这里。
# 想恢复：在 partials/sidebar.html 末尾加回一张卡片并 range site.Params.links。
[[links]]
  name = "GOOBLOG"
  url = "https://www.blog.googookuki.cn/#/home"

# ---- 侧栏：推荐文章 ----
# 用标签驱动：给文章 front matter 的 tags 加上下面这个标签名，
# 它就会进侧栏的「推荐文章」卡片（按日期倒序，最多 5 条）。
# 一篇都没打该标签时，卡片回落显示「最新文章」。
[recommend]
  tag = "推荐文章"

# ---- 暗色模式 ----
[darkMode]
  enabled = true
  # auto | light | dark
  default = "auto"
  # 显示切换按钮
  toggle = true
