# ============================================================
#  站点基础配置
#  latest 分支：纯模板 Hugo 站点，无历史数据
# ============================================================

baseURL = "https://blog.zhtsu.cc/"
title = "摆大新青年"
languageCode = "zh-cn"
defaultContentLanguage = "zh-cn"

# 中文内容必须开启，否则 .Summary / .WordCount 对 CJK 判断失准
hasCJKLanguage = true

# 分页：文章列表每页条数
paginate = 8

# 生成摘要，供列表卡片使用
summaryLength = 80

[taxonomies]
  tag = "tags"
  category = "categories"

# 输出格式定义：供前端全文检索使用的 JSON 索引 -> /search.json
[outputFormats.SearchIndex]
  mediaType = "application/json"
  baseName = "search"
  isPlainText = true
  notAlternative = true

[outputs]
  home = ["HTML", "RSS", "SearchIndex"]

[sitemap]
  changefreq = "weekly"
  priority = 0.5
  filename = "sitemap.xml"
